<ci:ComponentBase
    x:Class="PyExecutor.HitokotoControl"
    x:TypeArguments="settings:PythonComponentSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ci="http://classisland.tech/schemas/xaml/core"
    xmlns:settings="clr-namespace:PyExecutor.Settings"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ci:ComponentBase.ContextMenu>
        <ContextMenu>
            <MenuItem Header="立即执行"
                      Click="RunNowMenuItem_OnClick">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="PlayCircle" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </ci:ComponentBase.ContextMenu>
    <TextBlock x:Name="HitokotoText"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="{DynamicResource MainWindowLargeFontSize}"
               TextWrapping="Wrap"
               TextAlignment="Center" />
</ci:ComponentBase>
